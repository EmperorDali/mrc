from distutils.core import setup
setup(
  name = 'mrc',
  packages = ['mrc'], # this must be the same as the name above
  version = '0.1',
  description = 'Read and write MRC files',
  author = 'David Dynerman',
  author_email = 'david@block-party.net',
  url = 'https://github.com/emperordali/mrc', # use the URL to the github repo
  download_url = 'https://github.com/emperordali/mrc/tarball/0.1', # I'll explain this in a second
  keywords = ['biology', 'em' ], # arbitrary keywords
  classifiers = [],
)
